# Splunk configuration

How to configure the **Splunk** data source (`grafana-splunk-datasource`) in Grafana.

For more information, see the [official documentation](https://grafana.com/docs/plugins/grafana-splunk-datasource).

> This page is generated from [`dsconfig.json`](dsconfig.json). Do not edit it by hand — run `go generate ./...` to refresh.

## Configuration sections

- [Connection](#connection)
- [Authentication](#authentication)
- [TLS settings](#tls-settings) — _optional_
- [Advanced HTTP settings](#advanced-http-settings) — _optional_
- [Advanced options](#advanced-options) — _optional_
- [Data links](#data-links) — _optional_

## Connection

### URL

_**required** · string_

Specify a complete HTTP URL (for example https://example.com:8080).

| | |
|---|---|
| Example | `URL` |

## Authentication

### Authentication method

_optional · select_

Choose an authentication method to access the data source.

| | |
|---|---|
| Default | `BasicAuth` |
| Allowed values | `BasicAuth` (Basic authentication), `custom-splunk` (Alternative authentication), `OAuthForward` (Forward OAuth Identity) |

### User

_conditionally required · string_

The username of the data source account.

| | |
|---|---|
| Example | `User` |
| Shown when | `jsonData_authType == 'BasicAuth' || jsonData_authType == ''` |

### Password

_🔒 secret (write-only) · conditionally required · string_

The password of the data source account.

| | |
|---|---|
| Example | `Password` |
| Shown when | `jsonData_authType == 'BasicAuth' || jsonData_authType == ''` |

### Authentication token

_🔒 secret (write-only) · conditionally required · multiline text_

Instead of providing a username and password, use an authentication token generated in Splunk dashboard.

| | |
|---|---|
| Example | `Enter authentication token` |
| Shown when | **Authentication method** is **Alternative authentication** (`custom-splunk`) |

**Authentication token**

Authentication tokens are a method for authenticating Splunk platform users into the Splunk platform. Tokens let you provide access to environments without having to provide the standard types of credentials. Read more in [Splunk documentation](https://docs.splunk.com/Documentation/Splunk/9.0.4/Security/CreateAuthTokens).

## TLS settings

Additional security measures that can be applied on top of authentication

_This section is optional._

### Add self-signed certificate

_optional · toggle_

Add your own Certificate Authority (CA) certificate on top of one generated by the certificate authorities for additional security measures.

| | |
|---|---|
| Default | `false` |

### CA Certificate

_🔒 secret (write-only) · conditionally required · multiline text_

Your self-signed certificate.

| | |
|---|---|
| Example | `Begins with --- BEGIN CERTIFICATE ---` |
| Shown when | **Add self-signed certificate** is `true` |

### TLS Client Authentication

_optional · toggle_

Validate using TLS client authentication, in which the server authenticates the client.

| | |
|---|---|
| Default | `false` |

### ServerName

_conditionally required · string_

A Servername is used to verify the hostname on the returned certificate.

| | |
|---|---|
| Example | `domain.example.com` |
| Shown when | **TLS Client Authentication** is `true` |

### Client Certificate

_🔒 secret (write-only) · conditionally required · multiline text_

The client certificate can be generated from a Certificate Authority or be self-signed.

| | |
|---|---|
| Example | `Begins with --- BEGIN CERTIFICATE ---` |
| Shown when | **TLS Client Authentication** is `true` |

### Client Key

_🔒 secret (write-only) · conditionally required · multiline text_

The client key can be generated from a Certificate Authority or be self-signed.

| | |
|---|---|
| Example | `Begins with --- RSA PRIVATE KEY CERTIFICATE ---` |
| Shown when | **TLS Client Authentication** is `true` |

### Skip TLS certificate validation

_optional · toggle_

Skipping TLS certificate validation is not recommended unless absolutely necessary or for testing.

| | |
|---|---|
| Default | `false` |

## Advanced HTTP settings

_This section is optional._

### Allowed cookies

_optional · list_

Grafana proxy deletes forwarded cookies by default. Specify cookies by name that should be forwarded to the data source.

| | |
|---|---|
| Example | `New cookie (hit enter to add)` |

### Timeout

_optional · number_

HTTP request timeout in seconds.

| | |
|---|---|
| Example | `Timeout in seconds` |

### Custom HTTP Headers

_optional · list_

Additional HTTP headers sent with every request. Header names are stored in jsonData (httpHeaderName<N>); header values are write-only in secureJsonData (httpHeaderValue<N>).

Each item has the following fields:

#### Header

_**required** · string_

| | |
|---|---|
| Example | `X-Custom-Header` |
| Must match | `^[A-Za-z][A-Za-z0-9-]*$` |

#### Value

_optional · string_

| | |
|---|---|
| Example | `Header Value` |

## Advanced options

_This section is optional._

### Limit number of results

_optional · number_

Set the `count` limit for returned results from each data request. When set to 0, there is no limit.

| | |
|---|---|
| Default | `0` |

### Enable preview mode

_optional · toggle_

Displays events as soon as the event is returned, instead of waiting until all of the events are returned.

| | |
|---|---|
| Default | `false` |

### Enable async queries

_optional · toggle_

Run query and periodically check for results.

| | |
|---|---|
| Default | `false` |

### Min

_optional · string_

Sets the bounds for async query poll interval. Each poll will run a random number milliseconds from [min, max).

| | |
|---|---|
| Example | `500` |
| Shown when | `jsonData_pollSearchResult == true || jsonData_previewMode == true` |

### Max

_optional · string_

Sets the bounds for async query poll interval. Each poll will run a random number milliseconds from [min, max).

| | |
|---|---|
| Example | `3000` |
| Shown when | `jsonData_pollSearchResult == true || jsonData_previewMode == true` |

### Auto cancel timeout

_optional · string_

Jobs automatically cancel after this many seconds of inactivity. Set to 0 to never auto cancel.

| | |
|---|---|
| Example | `30` |

### Timeout in seconds

_optional · number_

Grafana request timeout. This is the maximum time in seconds that a query can run before it is killed. Minimum value is 1.

| | |
|---|---|
| Default | `30` |
| Example | `30` |

### Set maximum status buckets

_optional · string_

Maximum number of timeline status buckets generated per query. Set to 0 to disable timelines.

| | |
|---|---|
| Example | `30` |

### Internal fields filtration

_optional · toggle_

By default hides fields with names that start with ‘_’. This filter can be customized using the Internal field pattern option.

| | |
|---|---|
| Default | `false` |

### Internal field pattern

_optional · string_

Regular expression defining the filter for filtering out internal fields.

| | |
|---|---|
| Example | `^_.+` |
| Shown when | **Internal fields filtration** is `true` |

### Set time stamp field

_optional · string_

Field used by Grafana to determine event timestamps.

| | |
|---|---|
| Default | `_time` |
| Example | `_time` |

### Set fields search mode

_optional · select_

Set the Search Mode for Fields.

| | |
|---|---|
| Default | `quick` |
| Allowed values | `quick`, `full` |

### Set variables search mode

_optional · select_

Set the Search Mode for Variables.

| | |
|---|---|
| Default | `fast` |
| Allowed values | `fast`, `smart`, `verbose` |

### Set default earliest time

_optional · string_

Earliest time for searches without time range (such as template variable queries).

| | |
|---|---|
| Example | `-1hr` |

## Data links

_This section is optional._

### Data links

_optional · list_

Add links to existing fields. Links will be shown in log row details next to the field value.

Each item has the following fields:

#### Field

_**required** · string_

Can be exact field name or a regex pattern that will match on the field name.

#### Label

_optional · string_

Use to provide a meaningful label to the data matched in the regex.

#### Regex

_**required** · string_

Use to parse and capture some part of the log message. You can use the captured groups in the template.

#### URL

_optional · string_

| | |
|---|---|
| Example | `http://example.com/${__value.raw}` |

#### Internal link

_optional · string_

UID of a Grafana data source. When set, the derived data link becomes an internal link to that data source and the URL is interpreted as a query.

## Other settings

### oauthPassThru

_optional · boolean_

Set to true only when the 'Forward OAuth Identity' (OAuthForward) authentication method is selected. Written by the config editor alongside jsonData.authType (SplunkAuthComponent.tsx:14-23); consumed by Grafana core to forward the user's OAuth token, not by the plugin backend. The OAuthForward method is only offered in the editor when the 'splunkEnableOAuthForwarding' feature toggle is enabled.

| | |
|---|---|
| Default | `false` |

### streamMode

_optional · boolean_

Legacy 'stream mode' flag no longer rendered by the config editor. The backend migrates a true value into previewMode (pkg/models/settings.go:103-105) and the frontend query builder also treats it as preview mode (src/datasource/query.ts:267). Preserved for round-trip fidelity with older datasources.

| | |
|---|---|
| Default | `false` |

