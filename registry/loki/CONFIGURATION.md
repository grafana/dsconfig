# Loki configuration

How to configure the **Loki** data source (`loki`) in Grafana.

For more information, see the [official documentation](https://grafana.com/docs/grafana/latest/datasources/loki/).

> This page is generated from [`dsconfig.json`](dsconfig.json). Do not edit it by hand — run `go generate ./...` to refresh.

## Configuration sections

- [Connection](#connection)
- [Authentication](#authentication)
- [TLS settings](#tls-settings) — _optional_
- [Advanced HTTP settings](#advanced-http-settings) — _optional_
- [Alerting](#alerting) — _optional_
- [Queries](#queries) — _optional_
- [Derived fields](#derived-fields) — _optional_

## Connection

### URL

_**required** · string_

| | |
|---|---|
| Example | `http://localhost:3100` |

## Authentication

### Authentication method

_optional · select_

Choose an authentication method to access the data source.

| | |
|---|---|
| Default | `NoAuth` |
| Allowed values | `BasicAuth` (Basic authentication), `OAuthForward` (Forward OAuth Identity), `NoAuth` (No Authentication) |

### User

_conditionally required · string_

The username of the data source account.

| | |
|---|---|
| Example | `User` |
| Shown when | **Authentication method** is **Basic authentication** (`BasicAuth`) |
| Required when | **basicAuth** is `true` |

### Password

_🔒 secret (write-only) · conditionally required · string_

The password of the data source account.

| | |
|---|---|
| Example | `Password` |
| Shown when | **Authentication method** is **Basic authentication** (`BasicAuth`) |
| Required when | **basicAuth** is `true` |

## TLS settings

_This section is optional._

### Add self-signed certificate

_optional · toggle_

Add your own Certificate Authority (CA) certificate on top of one generated by the certificate authorities for additional security measures.

| | |
|---|---|
| Default | `false` |

### CA Certificate

_🔒 secret (write-only) · conditionally required · multiline text_

Your self-signed certificate.

| | |
|---|---|
| Example | `Begins with --- BEGIN CERTIFICATE ---` |
| Shown when | **Add self-signed certificate** is `true` |

### TLS Client Authentication

_optional · toggle_

Validate using TLS client authentication, in which the server authenticates the client.

| | |
|---|---|
| Default | `false` |

### ServerName

_conditionally required · string_

A Servername is used to verify the hostname on the returned certificate.

| | |
|---|---|
| Example | `domain.example.com` |
| Shown when | **TLS Client Authentication** is `true` |

### Client Certificate

_🔒 secret (write-only) · conditionally required · multiline text_

The client certificate can be generated from a Certificate Authority or be self-signed.

| | |
|---|---|
| Example | `Begins with --- BEGIN CERTIFICATE ---` |
| Shown when | **TLS Client Authentication** is `true` |

### Client Key

_🔒 secret (write-only) · conditionally required · multiline text_

The client key can be generated from a Certificate Authority or be self-signed.

| | |
|---|---|
| Example | `Begins with --- RSA PRIVATE KEY CERTIFICATE ---` |
| Shown when | **TLS Client Authentication** is `true` |

### Skip TLS certificate validation

_optional · toggle_

Skipping TLS certificate validation is not recommended unless absolutely necessary or for testing.

| | |
|---|---|
| Default | `false` |

## Advanced HTTP settings

_This section is optional._

### Allowed cookies

_optional · list_

Grafana proxy deletes forwarded cookies by default. Specify cookies by name that should be forwarded to the data source.

| | |
|---|---|
| Example | `New cookie (hit enter to add)` |

### Timeout

_optional · number_

HTTP request timeout in seconds.

| | |
|---|---|
| Example | `Timeout in seconds` |

### Custom HTTP Headers

_optional · list_

Additional HTTP headers sent with every request. Header names are stored in jsonData (httpHeaderName<N>); header values are write-only in secureJsonData (httpHeaderValue<N>).

Each item has the following fields:

#### Header

_**required** · string_

| | |
|---|---|
| Example | `X-Custom-Header` |
| Must match | `^[A-Za-z][A-Za-z0-9-]*$` |

#### Value

_optional · string_

| | |
|---|---|
| Example | `Header Value` |

## Alerting

_This section is optional._

### Manage alert rules in Alerting UI

_optional · toggle_

Manage alert rules for this data source. To manage other alerting resources, add an Alertmanager data source.

## Queries

_This section is optional._

### Maximum lines

_optional · string_

Loki queries must contain a limit of the maximum number of lines returned (default: 1000). Increase this limit to have a bigger result set for ad-hoc analysis. Decrease this limit if your browser becomes sluggish when displaying the log results.

| | |
|---|---|
| Example | `1000` |

## Derived fields

_This section is optional._

### Derived fields

_optional · list_

Derived fields can be used to extract new fields from a log message and create a link from its value.

Each item has the following fields:

#### Name

_**required** · string_

| | |
|---|---|
| Example | `Field name` |

#### Type

_optional · select_

Derived fields can be created from labels or by applying a regular expression to the log message.

| | |
|---|---|
| Default | `regex` |
| Allowed values | `regex` (Regex in log line), `label` (Label) |

#### Regex

_**required** · string_

Use to parse and capture some part of the log message. You can use the captured groups in the template.

#### URL

_optional · string_

| | |
|---|---|
| Example | `http://example.com/${__value.raw}` |

#### URL Label

_optional · string_

Use to override the button label when this derived field is found in a log.

#### Data source

_optional · string_

The tracing data source this internal link should navigate to. Setting this makes the derived field an internal link.

#### Open in new tab

_optional · toggle_

