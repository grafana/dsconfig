# Elasticsearch configuration

How to configure the **Elasticsearch** data source (`elasticsearch`) in Grafana.

For more information, see the [official documentation](https://grafana.com/docs/grafana/latest/datasources/elasticsearch/).

> This page is generated from [`dsconfig.json`](dsconfig.json). Do not edit it by hand — run `go generate ./...` to refresh.

## Configuration sections

- [Connection](#connection)
- [Authentication](#authentication)
- [TLS settings](#tls-settings) — _optional_
- [Additional settings](#additional-settings) — _optional_
- [Elasticsearch details](#elasticsearch-details)
- [Logs](#logs) — _optional_
- [Data links](#data-links) — _optional_

## Connection

### URL

_**required** · string_

Specify a complete HTTP URL (for example https://example.com:8080).

| | |
|---|---|
| Example | `http://localhost:9200` |

## Authentication

### Authentication method

_optional · select_

Choose an authentication method to access the data source.

| | |
|---|---|
| Default | `NoAuth` |
| Allowed values | `BasicAuth` (Basic authentication), `OAuthForward` (Forward OAuth Identity), `NoAuth` (No Authentication), `custom-api-key` (API Key), `custom-sigv4` (SigV4 auth) |

### User

_conditionally required · string_

The username of the data source account.

| | |
|---|---|
| Example | `User` |
| Shown when | **Authentication method** is **Basic authentication** (`BasicAuth`) |
| Required when | **basicAuth** is `true` |

### Password

_🔒 secret (write-only) · conditionally required · string_

The password of the data source account.

| | |
|---|---|
| Example | `Password` |
| Shown when | **Authentication method** is **Basic authentication** (`BasicAuth`) |
| Required when | **basicAuth** is `true` |

### API Key

_🔒 secret (write-only) · conditionally required · string_

API Key authentication.

| | |
|---|---|
| Example | `Enter your API key` |
| Shown when | **Authentication method** is **API Key** (`custom-api-key`) |
| Required when | **apiKeyAuth** is `true` |

## TLS settings

_This section is optional._

### Add self-signed certificate

_optional · toggle_

Add your own Certificate Authority (CA) certificate on top of one generated by the certificate authorities for additional security measures.

| | |
|---|---|
| Default | `false` |

### CA Certificate

_🔒 secret (write-only) · conditionally required · multiline text_

Your self-signed certificate.

| | |
|---|---|
| Example | `Begins with --- BEGIN CERTIFICATE ---` |
| Shown when | **Add self-signed certificate** is `true` |

### TLS Client Authentication

_optional · toggle_

Validate using TLS client authentication, in which the server authenticates the client.

| | |
|---|---|
| Default | `false` |

### ServerName

_conditionally required · string_

A Servername is used to verify the hostname on the returned certificate.

| | |
|---|---|
| Example | `domain.example.com` |
| Shown when | **TLS Client Authentication** is `true` |

### Client Certificate

_🔒 secret (write-only) · conditionally required · multiline text_

The client certificate can be generated from a Certificate Authority or be self-signed.

| | |
|---|---|
| Example | `Begins with --- BEGIN CERTIFICATE ---` |
| Shown when | **TLS Client Authentication** is `true` |

### Client Key

_🔒 secret (write-only) · conditionally required · multiline text_

The client key can be generated from a Certificate Authority or be self-signed.

| | |
|---|---|
| Example | `Begins with --- RSA PRIVATE KEY CERTIFICATE ---` |
| Shown when | **TLS Client Authentication** is `true` |

### Skip TLS certificate validation

_optional · toggle_

Skipping TLS certificate validation is not recommended unless absolutely necessary or for testing.

| | |
|---|---|
| Default | `false` |

## Additional settings

_This section is optional._

### Allowed cookies

_optional · list_

Grafana proxy deletes forwarded cookies by default. Specify cookies by name that should be forwarded to the data source.

| | |
|---|---|
| Example | `New cookie (hit enter to add)` |

### Timeout

_optional · number_

HTTP request timeout in seconds.

| | |
|---|---|
| Example | `Timeout in seconds` |

### Custom HTTP Headers

_optional · list_

Additional HTTP headers sent with every request. Header names are stored in jsonData (httpHeaderName<N>); header values are write-only in secureJsonData (httpHeaderValue<N>).

Each item has the following fields:

#### Header

_**required** · string_

| | |
|---|---|
| Example | `X-Custom-Header` |
| Must match | `^[A-Za-z][A-Za-z0-9-]*$` |

#### Value

_optional · string_

| | |
|---|---|
| Example | `Header Value` |

## Elasticsearch details

### Index name

_**required** · string_

Name of your Elasticsearch index. You can use a time pattern, such as YYYY.MM.DD, or a wildcard for the index name.

| | |
|---|---|
| Example | `es-index-name` |

### Pattern

_optional · select_

If you're using a pattern for your index, select the type, or no pattern.

| | |
|---|---|
| Allowed values | `` (No pattern), `Hourly`, `Daily`, `Weekly`, `Monthly`, `Yearly` |

### Time field name

_**required** · string_

Name of your time field. Defaults to @timestamp.

| | |
|---|---|
| Default | `@timestamp` |
| Example | `@timestamp` |

### Max concurrent Shard Requests

_optional · number_

Maximum number of concurrent shards a search request can hit per node. Defaults to 5.

| | |
|---|---|
| Default | `5` |

### Min time interval

_optional · string_

A lower limit for the auto group by time interval. Recommended to be set to write frequency, for example 1m if your data is written every minute.

| | |
|---|---|
| Example | `10s` |
| Must match | `^\d+(ms|[Mwdhmsy])$` |

### Include Frozen Indices

_optional · toggle_

Include frozen indices in searches.

| | |
|---|---|
| Default | `false` |

### Default query mode

_optional · select_

Default query mode to use for the data source. Defaults to 'Metrics'.

| | |
|---|---|
| Default | `metrics` |
| Allowed values | `metrics` (Metrics), `logs` (Logs), `raw_data` (Raw Data), `raw_document` (Raw Document) |

## Logs

_This section is optional._

### Message field name

_optional · string_

Configure the field to be used for log messages.

| | |
|---|---|
| Example | `_source` |

### Level field name

_optional · string_

Configure the field that determines the level of each log message.

## Data links

_This section is optional._

### Data links

_optional · list_

Add links to existing fields. Links will be shown in log row details next to the field value.

Each item has the following fields:

#### Field

_**required** · string_

Can be exact field name or a regex pattern that will match on the field name.

#### URL/Query

_optional · string_

| | |
|---|---|
| Example | `http://example.com/${__value.raw}` |

#### URL Label

_optional · string_

Use to override the button label.

#### Internal link

_optional · string_

The tracing data source the data link should navigate to. When set, the link becomes an internal Grafana link and the url field is treated as a query for that data source.

